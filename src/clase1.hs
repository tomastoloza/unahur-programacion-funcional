doble x = 2 * x
suma x y = x + y
producto x y = x * y
cuadrado x = x ^ 2
constante x = x
f1 x1 x2 = sqrt((x1^2) + (x2^2))

absoluto x | x >= 0    = x
           | otherwise = -x

absoluto' x = if x >= 0 then x else -x

